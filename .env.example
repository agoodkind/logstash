# Logstash Configuration
LOGSTASH_USER=logstash
LOGSTASH_GROUP=logstash
SYSLOG_PORT=5140

# Remote Deployment (SSH)
# Set REMOTE=1 to enable remote deployment
REMOTE_HOST=root@logstash.home.goodkind.io

# Proxmox Container Deployment
# Set both to enable Proxmox pct deployment
PROXMOX_HOST=root@vault.home.goodkind.io
PROXMOX_VMID=100

# Development Options
# DRY_RUN=1        # Preview commands without executing
# VERBOSE=1        # Show detailed output
# REMOTE=1         # Enable remote SSH deployment
